import time
import random


def sum(A, n):
    # code here
    for i in range(n):
        for j in range(n):

            # n = 1 이상 5000 이하 정수 값 입력
            # 리스트 A에 랜덤 정수 값 n개 채움
            # sum 함수 호출 + 시간 측정
            # 함수의 수행시간을 출력
